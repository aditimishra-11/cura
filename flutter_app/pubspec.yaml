name: cura
description: Cura - save and retrieve content with AI

publish_to: none
version: 1.0.4+6

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  shared_preferences: ^2.2.2
  receive_sharing_intent: ^1.8.0
  flutter_markdown: ^0.6.22
  intl: ^0.19.0
  firebase_core: ^2.27.0
  firebase_messaging: ^14.7.20
  flutter_local_notifications: ^16.3.2
  url_launcher: ^6.2.5
  google_fonts: ^6.2.1
  google_sign_in: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
